package pw.x4.ninety.data

import pw.x4.ninety.data.persistence.PersistedNode
import pw.x4.ninety.data.persistence.PersistedProfile

internal fun Node.toPersistedNode(): PersistedNode = PersistedNode(
    id = id,
    profileId = subId,
    protocol = proto,
    name = name,
    host = host,
    port = port,
    uuid = uuid,
    password = password,
    method = method,
    cipher = cipher,
    alterId = alterId,
    security = security,
    transport = type,
    flow = flow,
    sni = sni,
    fingerprint = fp,
    publicKey = pbk,
    shortId = sid,
    alpn = alpn,
    path = path,
    hostHeader = hostHeader,
    serviceName = serviceName,
    mode = mode,
    extra = extra,
    upMbps = upMbps,
    downMbps = downMbps,
    obfs = obfs,
    obfsPassword = obfsPassword,
    certificatePublicKeySha256 = pinSHA256,
    congestionControl = congestion,
    udpRelayMode = udpRelay,
    insecure = insecure,
    zeroRttHandshake = zeroRtt,
    disableSni = disableSni,
    plugin = plugin,
    pluginOptions = pluginOpts,
    raw = raw,
    fromSubscription = fromSub,
)

internal fun PersistedNode.toLegacyNode(): Node = Node(
    proto = protocol,
    name = name,
    host = host,
    port = port,
    uuid = uuid,
    password = password,
    method = method,
    cipher = cipher,
    alterId = alterId,
    security = security,
    type = transport,
    flow = flow,
    sni = sni,
    fp = fingerprint,
    pbk = publicKey,
    sid = shortId,
    alpn = alpn,
    path = path,
    hostHeader = hostHeader,
    serviceName = serviceName,
    mode = mode,
    extra = extra,
    upMbps = upMbps,
    downMbps = downMbps,
    obfs = obfs,
    obfsPassword = obfsPassword,
    pinSHA256 = certificatePublicKeySha256,
    congestion = congestionControl,
    udpRelay = udpRelayMode,
    insecure = insecure,
    zeroRtt = zeroRttHandshake,
    disableSni = disableSni,
    plugin = plugin,
    pluginOpts = pluginOptions,
    raw = raw,
    fromSub = fromSubscription,
    subId = profileId,
)

internal fun Profile.toPersistedProfile(): PersistedProfile = PersistedProfile(
    id = id,
    name = name,
    type = type,
    url = url,
    used = used,
    total = total,
    expire = expire,
    updatedAt = updatedAt,
)

internal fun PersistedProfile.toLegacyProfile(): Profile = Profile(
    id = id,
    name = name,
    type = type,
    url = url,
    used = used,
    total = total,
    expire = expire,
    updatedAt = updatedAt,
)
